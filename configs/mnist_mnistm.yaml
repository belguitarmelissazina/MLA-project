source: mnist
target: mnistm
data_root: ./datasets
epochs: 100
batch_size: 128
lr: 0.01
momentum: 0.9
weight_decay: 0.0
gamma: 10.0
lr_alpha: 10.0
lr_beta: 0.75
pretrained: false
experiment_name: mnist_to_mnistm
checkpoint_dir: ./checkpoints
results_dir: ./results
save_freq: 10
num_workers: 4
train_dann: true
train_source_only: true
seed: 42
