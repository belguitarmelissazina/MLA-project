source: svhn
target: mnist
data_root: ./datasets
epochs: 100
batch_size: 128
lr: 0.005
momentum: 0.9
weight_decay: 0.0
gamma: 5.0
lr_alpha: 0.0
lr_beta: 0.0
pretrained: false
experiment_name: svhn_to_mnist
checkpoint_dir: ./checkpoints
results_dir: ./results
save_freq: 10
num_workers: 4
train_dann: true
train_source_only: true
seed: 42
